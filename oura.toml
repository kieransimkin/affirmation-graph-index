[source]
type = "N2N"
address = ["Tcp", "localhost:6000"]
magic = "preprod"
min_depth = 6

[[filters]]
type = "Selection"

[filters.check]
predicate = "all_of"

[[filters.check.argument]]
predicate = "variant_in"
argument = ["Transaction", "Mint"]

[[filters.check.argument]]
predicate = "policy_equals"
argument = "bdc16c0437b25cc7d9addd100cfeb211d8e7fceb14a12c794feca824"

[source.mapper]
include_transaction_details = true


[sink]
type = "Terminal"
throttle_min_span_millis = 500
wrap = false


[retries]
max_retries = 3
backoff_unit_sec = 10
backoff_factor = 3
max_backoff_sec = 10
dismissible = true